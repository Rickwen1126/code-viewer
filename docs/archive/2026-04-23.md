# Todo — 2026-04-23

## Backlog

- **File tree lazy load**
  - 現況：`readDirectoryRecursive` 一次遞迴到 depth 10，開 `~` 等大目錄會爆
  - 目標：改成只載入當前展開的那一層，展開時再 fetch children（跟 VS Code Explorer 一樣）
  - 影響範圍：extension `file-provider.ts`、shared `FileTreeNode` type、frontend file tree component
