# Desktop UI Design — Code Viewer

Created: 2026-04-13
Last Updated: 2026-04-16
Status: Draft

---

## 1. Design Goal

把目前 mobile-first 的單欄 UI 擴展為 desktop layout，**充分利用橫向空間**讓 tree + file、commit list + diff、tour list + step detail 能同時顯示，不再需要在頁面間來回切換。

Mobile layout 完全不動 — desktop 是在 `>= 1024px` 時切換到新的 shell 結構。

---

## 2. Current Mobile Architecture (as-is)

```
┌──────────────────────────────────┐
│         ConnectionStatus         │
├──────────────────────────────────┤
│                                  │
│        <main> (Routes)           │
│   Full-page, single column       │
│   route-based navigation         │
│                                  │
├──────────────────────────────────┤
│  [Workspaces] [Files] [Git] [Tours]  │
│         Bottom TabBar (56px)     │
└──────────────────────────────────┘
```

**Navigation model**: 每個 tab 是獨立的 page stack。從 `/files` 點檔案 → navigate 到 `/files/path`（整頁替換）。返回靠 swipe-right 手勢或 browser back。

**State management (2026-04-14 重構後)**:
- 所有 per-workspace 狀態以 `workspaceKey`（opaque stable ID）為主鍵，取代舊的 `extensionId`
- State services 已 extract 成獨立 module：`current-file.ts`, `file-scroll.ts`, `last-location.ts`, `selected-workspace.ts`
- `LastLocationTracker` 在每次 navigation 自動記錄語義位置（tab + route + workspace）
- Session restore 優先順序：LastLocation (semantic) > CurrentFile (convenience) > /files

**Key pain point on desktop**: 看完 code 要回 tree 選另一個檔案，得按 back 回去。Git、Tours 同理。

---

## 3. Desktop Shell Architecture

```
┌──────────┬────────────────────┬──────────────────────────────────┐
│ Activity │   Sidebar Panel    │       Main Content Area          │
│   Bar    │   (resizable)      │                                  │
│  48px    │   240–400px        │       flex: 1                    │
│          │   default: 280px   │                                  │
│          │                    │                                  │
│  ┌────┐  │                    │                                  │
│  │ 📁 │  │   Context-         │                                  │
│  │ 🔀 │  │   dependent        │   Detail / viewer area           │
│  │ 🗺 │  │   list panel       │   changes per route              │
│  │    │  │                    │                                  │
│  │    │  │                    │                                  │
│  │    │  │                    │                                  │
│  │    │  │                    │                                  │
│  └────┘  │                    │                                  │
│  ┌────┐  │                    │                                  │
│  │ WS │  │                    │                                  │
│  └────┘  │                    │                                  │
└──────────┴────────────────────┴──────────────────────────────────┘
```

### 3.1 Activity Bar (左側 icon rail)

取代 mobile 的 bottom tab bar。

| 位置 | 內容 | 說明 |
|------|------|------|
| 上方 | Files / Git / Tours icons | 垂直排列，active 時左側 2px blue border (#569cd6) |
| 下方 | Workspace 選擇器 | 顯示 workspace 首字母 avatar，click 開 popover |

**尺寸**: 48px 寬
**背景**: #252526（比 sidebar 的 #1e1e1e 略深，製造層次感）
**Active 指示**: 左側 2px solid #569cd6 + icon 變亮 (#d4d4d4 → #fff)
**Hover**: icon 背景 #2a2d2e

### 3.2 Sidebar Panel

根據 active tab 顯示不同內容：

| Active Tab | Sidebar 內容 | 對應現有 component |
|------------|-------------|-------------------|
| Files | File tree + search + recent + bookmarks | `FileBrowserPage` |
| Git | Branch info + staged/unstaged + commit list | `GitChangesPage` |
| Tours | Tour list → drill into step list | `TourListPage` |

**寬度**: 280px default, 拖拉調整 (min 200px, max 480px)
**Resize handle**: 右邊緣 4px, `cursor: col-resize`
**Collapse**: 雙擊 resize handle 或 `Cmd+B` toggle
**Persistence**: 寬度存 localStorage `code-viewer:sidebar-width`

### 3.3 Main Content Area

顯示 detail/viewer，根據 route：

| Route | Main Content | 空狀態 |
|-------|-------------|--------|
| `/files` | Empty state: "Select a file" | 顯示 workspace 概要 |
| `/files/*` | Code viewer (語法高亮 + 所有現有功能) | — |
| `/git` | Empty state: "Select a commit or file" | — |
| `/git/diff/*` | Diff detail viewer (text diff + media preview) | — |
| `/tours` | Empty state: "Select a tour" | — |
| `/tours/:id` | Tour step viewer (markdown + code snippet) | — |
| `/open/file` | Workspace resolver → redirect to `/files/*` | — |
| `/open/tour` | Workspace resolver → redirect to `/tours/:id` | — |
| `/open/git-diff` | Workspace resolver → redirect to `/git/diff/*` | — |

**Deep link resolvers** (`/open/*`): 由外部連結觸發（CLI、VS Code、分享連結）。Desktop 遇到 `/open/*` 時 sidebar 保持穩定，main content 顯示 workspace resolver UI；若 workspace 已連線則直接 redirect。

---

## 4. Per-View Desktop Layout

### 4.1 Files View — Tree + Code 並列

```
┌────────┬──────────────────┬─────────────────────────────────────┐
│        │ Search files...  │ src/pages/files/code-viewer.tsx     │
│  [📁]  │ ─────────────── │ typescript    Wrap  Symbols  🔍    │
│  [🔀]  │ ▸ .tours/       │ ───────────────────────────────────│
│  [🗺]  │ ▸ backend/      │  1 │ import { useState } from ...  │
│        │ ▾ frontend/      │  2 │ import { useParams } from ... │
│        │   ▾ src/         │  3 │                               │
│        │     ▸ components/│  4 │ export function CodeViewer() { │
│        │     ▸ hooks/     │  5 │   const { path } = useParams() │
│        │     ▸ pages/     │  6 │   ...                          │
│        │     ▸ services/  │  7 │ }                              │
│        │                  │                                     │
│        │ RECENT           │                                     │
│        │  code-viewer.tsx │                                     │
│        │  app.tsx         │                                     │
│        │                  │                                     │
│  [WS]  │ BOOKMARKS        │                                     │
│        │  ★ ws-client:42  │                                     │
└────────┴──────────────────┴─────────────────────────────────────┘
```

**關鍵改變**:
- File tree 常駐 sidebar，不因開檔案而消失
- 點擊 tree 中的檔案 → main content 更新，sidebar 不動
- 當前檔案在 tree 中用 blue left border 高亮（mobile 已有此邏輯）
- Code viewer header 改用 breadcrumb 展示完整路徑：`frontend / src / pages / files / code-viewer.tsx`

### 4.2 Git View — Commits + Diff 並列

```
┌────────┬──────────────────┬─────────────────────────────────────┐
│        │ semantic-locati… │ src/app.tsx                         │
│  [📁]  │ 0 changed       │ ┌────────────────┬────────────────┐ │
│  [🔀]  │ ──────────────  │ │ OLD (a1b2c3)   │ NEW (828bdb4)  │ │
│  [🗺]  │ COMMITS (30)    │ │────────────────│────────────────│ │
│        │                  │ │  import { A }  │  import { A }  │ │
│        │ 828bdb4 docs:    │ │  import { B }  │  import { B }  │ │
│        │ stage feature…  │ │- import { C }  │+ import { D }  │ │
│        │ Rick · 2m       │ │  ...           │  ...           │ │
│        │                  │ │                │                │ │
│        │ 7f215f3 feat:    │ │                │                │ │
│        │ preview images…  │ └────────────────┴────────────────┘ │
│        │ Rick · 4h       │                                     │
│        │                  │ src/services/cache.ts               │
│        │ e7c14f2 fix:     │ ┌─────────────────────────────────┐ │
│        │ use opaque…     │ │ @@ -12,6 +12,8 @@               │ │
│        │ Rick · 6h       │ │  const db = ...                  │ │
│  [WS]  │                  │ │+ const newFn = ...               │ │
│        │ ...             │ └─────────────────────────────────┘ │
└────────┴──────────────────┴─────────────────────────────────────┘
```

**關鍵改變**:
- Commit list 常駐 sidebar
- 點擊 commit → main content 顯示該 commit 的所有 changed files + diff
- **Side-by-side diff**: 桌面空間足夠時用雙欄 diff（old | new），取代 mobile 的 unified diff
- 點擊 staged/unstaged file → 同樣在 main content 顯示 diff
- Sidebar 中的 commit 被選中時用 `background: #264f78` 高亮

### 4.3 Tours View — Tour List + Step Detail 並列

```
┌────────┬──────────────────┬─────────────────────────────────────┐
│        │ Code Tours       │ Step 1 / 8                         │
│  [📁]  │        + New Tour│ ──────────────────────────────────  │
│  [🔀]  │ ──────────────  │                                     │
│  [🗺]  │                  │ ## Context                          │
│        │ ▾ 01 - Sky Eye   │                                     │
│        │   8 steps        │ ### Code Viewer 是什麼              │
│        │   ┌─────────────┐│ 把 Desktop VS Code 的完整能力      │
│        │   │ 1. Context  ││ （檔案瀏覽、LSP、Git、Copilot）    │
│        │   │ 2. Frontend ││ 搬到手機上的 Mobile PWA。           │
│        │   │ 3. Backend  ││                                     │
│        │   │ 4. Extension││ ### 白話導讀                        │
│        │   │ 5. WS Proto ││ 這條 tour 其實是在看...            │
│        │   │ 6. ...      ││                                     │
│        │   └─────────────┘│ ┌─ ws-client.ts:42 ──────────────┐ │
│        │                  │ │ export function createWsClient() │ │
│        │ ▸ 02 - Edit Step │ │   const ws = new WebSocket(url)  │ │
│        │   5 steps        │ │   ...                            │ │
│  [WS]  │                  │ └────────────────────────────────┘ │
│        │ ▸ 03 - Module…  │        [◀ Prev]    [Next ▶]        │
└────────┴──────────────────┴─────────────────────────────────────┘
```

**關鍵改變**:
- Tour list 常駐 sidebar，展開 tour 可直接看到 step list
- 點擊 step → main content 顯示 step 內容 + code snippet
- Step 切換可在 sidebar 直接點選（取代 mobile 的 Prev/Next + dots）
- Main content 仍保留 Prev/Next 按鈕，但 dot indicators 可省略（sidebar 已有 step list）
- 當前 step 在 sidebar 用 blue left border 標示

### 4.4 Workspaces — Popover（非整頁）

```
┌────────┐
│        │  ┌──────────────────┐
│  [📁]  │  │  WorkProof       │
│  [🔀]  │  │  ● connected     │
│  [🗺]  │  │  ────────────── │
│        │  │  chatpilot       │
│        │  │  ● connected     │
│        │  │  ────────────── │
│        │  │  code-viewer ◀  │
│        │  │  ● connected     │
│  [WS]◀─┤  │  ────────────── │
│        │  │  notebooklm-…   │
│        │  │  ● connected     │
└────────┘  └──────────────────┘
```

- 桌面不需要整頁的 workspace 選擇器
- Activity bar 底部的 WS icon → click 開 popover（向右彈出）
- 選完 workspace 後 popover 自動收起
- 保持 `/workspaces` route 給 mobile 用

---

## 5. Responsive Breakpoint Strategy

| Viewport | Layout | Navigation |
|----------|--------|------------|
| < 768px | **Mobile** (current) | Bottom tab bar, full-page routes |
| >= 1024px | **Desktop** | Activity bar + sidebar + content |
| 768–1023px | **Compact desktop** | Activity bar + narrower sidebar (200px) + content |

偵測方式：CSS media query + JS `matchMedia` (for conditional rendering)

```css
@media (min-width: 1024px) {
  /* Desktop shell */
}
@media (min-width: 768px) and (max-width: 1023px) {
  /* Compact desktop — narrower sidebar */
}
```

在 React 層面，`TabLayout` 根據 `useIsDesktop()` hook 決定 render `MobileLayout` 或 `DesktopLayout`：

```tsx
// hooks/use-is-desktop.ts
function useIsDesktop(breakpoint = 1024) {
  const [isDesktop, setIsDesktop] = useState(
    () => window.matchMedia(`(min-width: ${breakpoint}px)`).matches
  )
  useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${breakpoint}px)`)
    const handler = (e: MediaQueryListEvent) => setIsDesktop(e.matches)
    mq.addEventListener('change', handler)
    return () => mq.removeEventListener('change', handler)
  }, [breakpoint])
  return isDesktop
}

// app.tsx
function TabLayout() {
  const isDesktop = useIsDesktop()
  return isDesktop ? <DesktopLayout /> : <MobileLayout />
}
```

- SPA，`window` 永遠存在，不用擔心 SSR hydration mismatch
- 初始值用 `window.matchMedia` 同步取，避免第一幀閃爍
- iPad 旋轉自動切換（portrait → mobile, landscape → desktop）

---

## 6. URL 與 History 策略

### 核心原則：URL 機制完全不動，desktop 只改 render 層

```
/files/src/app.tsx

Mobile:  整頁顯示 code viewer（file tree 被 unmount）
Desktop: sidebar 顯示 file tree + main 顯示 code viewer（同時存在）

URL 一樣、history stack 一樣、state services 一樣。
```

### Navigation 行為對照

| 操作 | Mobile | Desktop |
|------|--------|---------|
| Sidebar item click | `navigate()` push, 整頁替換 | `navigate()` push, main content 替換, sidebar 不動 |
| Tab switch | crossfade, push | activity bar 切換 + sidebar/main crossfade, push |
| Browser back | 回到上一個 URL（可能是 tab root） | 同左，但 tab root 顯示 landing page 而非空白 |
| Detour (tour → code) | push, 存 detour anchor | push, 同時切 activity bar tab + sidebar |
| Detour unwind | navigate back to anchor URL | 同左，tab + sidebar 自動跟著 URL 切換 |

### Tab root landing page（desktop 專屬）

Desktop back 到 `/files`、`/git`、`/tours` 時不顯示「Select a file」空白：

| Route | Desktop landing content |
|-------|------------------------|
| `/files` | Recent files + bookmarks（sidebar 已有 tree，main 區用更多空間展示） |
| `/git` | Branch overview + recent commit summary |
| `/tours` | Tour card view（比 sidebar list 更詳細的 overview） |

### View transition 對照

| Navigation 類型 | Mobile | Desktop |
|----------------|--------|---------|
| Tab switch | crossfade 150ms | sidebar + main crossfade, activity bar 不動 |
| Sidebar item click | slide-in-right 220ms | **instant**（sidebar 不動，只換 main） |
| Detour (跨 tab) | slide-in-right | crossfade（整個 view 都換） |
| Browser back | slide-out-right (reverse) | **instant**（或淡出 100ms） |

---

## 7. Routing Model Change

### Mobile (current, unchanged)
```
/workspaces        → WorkspacesPage (full page)
/files             → FileBrowserPage (full page)
/files/*           → CodeViewerPage (full page, replaces file browser)
/git               → GitChangesPage (full page)
/git/diff/*        → GitDiffDetailPage (full page)
/tours             → TourListPage (full page)
/tours/:id         → TourDetailPage (full page)
/open/file         → OpenFileResolverPage (workspace resolver → redirect)
/open/tour         → OpenTourResolverPage (workspace resolver → redirect)
/open/git-diff     → OpenGitDiffResolverPage (workspace resolver → redirect)
```

### Desktop (new)
```
/files             → Sidebar: FileBrowserPage | Main: EmptyState
/files/*           → Sidebar: FileBrowserPage | Main: CodeViewerPage
/git               → Sidebar: GitChangesPage  | Main: EmptyState
/git/diff/*        → Sidebar: GitChangesPage  | Main: GitDiffDetailPage
/tours             → Sidebar: TourListPage     | Main: EmptyState
/tours/:id         → Sidebar: TourListPage     | Main: TourDetailPage
/open/*            → Sidebar: 保持穩定          | Main: WorkspaceResolver → redirect
/workspaces        → Popover from activity bar (not routed)
```

**核心差異**: Desktop 不 unmount sidebar 內容。 Mobile 的「整頁替換」變成 desktop 的「main panel 替換 + sidebar 留存」。

**Session restore**: Desktop 開啟時利用 `LastLocationTracker` 存的語義位置自動恢復到上次的 tab + route + scroll position。State services 已用 `workspaceKey` 做 scoping，切 workspace 時所有持久化狀態自動跟著切換。

**Implementation**: `DesktopLayout` component 包含兩個 render slot:
```tsx
function DesktopLayout() {
  const activeTab = deriveActiveTab(location.pathname)
  return (
    <div style={{ display: 'flex', height: '100dvh' }}>
      <ActivityBar activeTab={activeTab} />
      <SidebarPanel activeTab={activeTab}>
        {activeTab === 'files' && <FileBrowserSidebar />}
        {activeTab === 'git' && <GitSidebar />}
        {activeTab === 'tours' && <ToursSidebar />}
      </SidebarPanel>
      <main style={{ flex: 1 }}>
        <Routes>
          <Route path="files" element={<FilesEmptyState />} />
          <Route path="files/*" element={<CodeViewerPage />} />
          <Route path="git" element={<GitEmptyState />} />
          <Route path="git/diff/*" element={<GitDiffDetailPage />} />
          <Route path="tours" element={<ToursEmptyState />} />
          <Route path="tours/:tourId" element={<TourDetailPage />} />
          {/* Deep link resolvers render in main content, sidebar stays stable */}
          <Route path="open/file" element={<OpenFileResolverPage />} />
          <Route path="open/tour" element={<OpenTourResolverPage />} />
          <Route path="open/git-diff" element={<OpenGitDiffResolverPage />} />
        </Routes>
      </main>
    </div>
  )
}
```

---

## 8. Code Structure Strategy — Fork Then Extract

### 8.1 決定：採用 Fork 模式

**同一個 frontend app、同一個 port (4801)**，`app.tsx` 是唯一的分流點，page-level code 各自獨立維護。

```
frontend/src/
  app.tsx                    ← useIsDesktop() → MobileLayout 或 DesktopLayout（唯一分流點）
  layouts/
    mobile/                  ← 現有 TabLayout + mobile pages（fork 過去，不動）
      tab-bar.tsx
      pages/                 ← 現有 mobile page components
    desktop/                 ← fork 出來的 desktop shell + desktop pages
      activity-bar.tsx
      sidebar-panel.tsx
      desktop-layout.tsx
      pages/                 ← desktop 版本的 page components
  services/                  ← 共用（WS, cache, state services）
  hooks/                     ← 共用（useWebSocket, useWorkspace, useIsDesktop）
  components/                ← 共用的純 UI（CodeBlock, DiffView, MarkdownRenderer）
```

### 8.2 兩種做法的比較與選擇理由

viewport 分流有兩種做法，本質上都是「根據螢幕寬度 render 不同 UI」，但實作意義不同：

**做法 A — 內部分支（不採用）**：每個 component 內部用 `isDesktop` 做條件分支

```tsx
// 一份 file-browser.tsx，內部分岔
function FileBrowserPage() {
  const isDesktop = useIsDesktop()
  return (
    <div style={{ padding: isDesktop ? 0 : 16 }}>
      {!isDesktop && <PullToRefresh />}
      <SearchBar size={isDesktop ? 'compact' : 'full'} />
      <FileTree onClick={isDesktop ? handleSidebarClick : handlePageClick} />
      {!isDesktop && <RecentFiles layout="list" />}
      {isDesktop && <RecentFiles layout="compact" />}
    </div>
  )
}
```

**做法 B — Fork page（採用）**：app.tsx 層級分流，page-level 獨立

```tsx
// app.tsx — 唯一分流點
function TabLayout() {
  const isDesktop = useIsDesktop()
  return isDesktop ? <DesktopLayout /> : <MobileLayout />
}

// layouts/mobile/pages/file-browser.tsx — 現有 code，不動
// layouts/desktop/pages/file-browser-sidebar.tsx — fork，自由改
```

**差異對照：**

| | 做法 A：內部分支 | 做法 B：Fork page |
|---|---|---|
| 分流點 | 散布在每個 component 內部 | 集中在 `app.tsx` 一處 |
| Mobile 風險 | 改 desktop 時可能 break mobile | 完全隔離，互不影響 |
| 閱讀 code | 要在腦中同時 parse 兩條路徑 | 每份 file 只有一條路徑 |
| 重複 code | 少（但 complexity 高） | 多（但 complexity 低） |
| 收斂時機 | 一開始就要設計好 abstraction | 等兩邊穩定後再抽共用 |
| 適合階段 | 兩端 UI 差異小、行為已穩定 | 一端穩定、另一端還在摸索 |

**選擇 Fork 的理由**：Mobile 已經在用且不能壞，desktop 還在摸索階段需要自由度。Complexity 放在「file 數量多」比放在「每個 file 內部複雜」更容易管理。

### 8.3 收斂路徑

1. **Phase 1**: Fork pages，desktop 和 mobile pages 各自維護
2. **Phase 2**: Desktop 穩定後，識別重複的 data-fetching / state 邏輯，抽成 custom hooks
3. **Phase 3**: 考慮是否需要 `packages/core`（如果 hooks 數量多到值得獨立 package）

### 8.4 Per-page fork 說明

**FileBrowserPage → desktop/pages/file-browser-sidebar.tsx**
- Fork search bar + tree + recent + bookmarks
- 移除 pull-to-refresh
- 調整 padding（sidebar 空間較窄）

**GitChangesPage → desktop/pages/git-sidebar.tsx**
- Fork branch info + staged/unstaged + commit list
- Commit 展開時不顯示 inline diff（diff 移到 main content）
- 點擊 commit row → navigate to `/git/diff/*`

**TourListPage → desktop/pages/tours-sidebar.tsx**
- Fork tour list
- 新增：展開 tour → 顯示 step list（mobile 只在 detail page 有）
- 點擊 step → navigate to `/tours/:id?step=N`

### 7.4 可直接 reuse 的現有 State Services

Phase 2 state cleanup 已將 state 邏輯 extract 成獨立 service module，desktop components 可直接 import：

| Service | 用途 | Desktop 利用方式 |
|---------|------|-----------------|
| `selected-workspace.ts` | workspace 選擇持久化 | WorkspacePopover 直接用 |
| `current-file.ts` | 當前檔案 per-workspace | sidebar file tree highlight |
| `file-scroll.ts` | scroll position per-workspace per-file | code viewer scroll restore |
| `last-location.ts` | 語義位置（tab + route） | session restore 時回到正確的 tab + route |

所有 service 都用 `workspaceKey` 做 scoping，切 workspace 時狀態自動隔離。

### 7.5 新增 Components

| Component | 用途 |
|-----------|------|
| `ActivityBar` | 左側 icon rail |
| `SidebarPanel` | 可 resize 的 sidebar container |
| `ResizeHandle` | 拖拉調整 sidebar 寬度 |
| `WorkspacePopover` | Workspace 選擇 popover |
| `SideBySideDiff` | 雙欄 diff view（desktop 專用） |
| `MediaDiffPreview` | Media file before/after 比較（desktop 專用） |
| `EmptyState` | 各 tab 的空狀態提示 |
| `useIsDesktop` | `matchMedia` hook |

---

## 8. Desktop-Specific Interactions

### 8.1 Hover States

所有可互動元素加 hover 背景：

```
File tree item:    hover → background: #2a2d2e
Commit row:        hover → background: #2a2d2e
Tour item:         hover → background: #2a2d2e
Button:            hover → opacity: 0.85
Activity bar icon: hover → background: #2a2d2e
```

### 8.2 Keyboard Shortcuts

| Shortcut | Action |
|----------|--------|
| `Cmd+B` | Toggle sidebar collapse |
| `Cmd+P` | Focus file search (Files tab) |
| `Cmd+Shift+E` | Switch to Files tab |
| `Cmd+Shift+G` | Switch to Git tab |
| `Cmd+Shift+T` | Switch to Tours tab |
| `Escape` | Close popover / search |

### 8.3 移除的 Mobile 行為

Desktop 上不需要：
- Left-edge swipe back（有 sidebar，不需要 back 手勢）
- Pull-to-refresh（可改用按鈕 / `Cmd+R`）
- Pinch zoom（桌面有 `Cmd+/-` 或設定字體大小）
- Bottom safe area inset（桌面沒有 home indicator）

---

## 9. Visual Design

### 9.1 Color Palette（維持 VS Code Dark 語彙）

```
Activity Bar bg:     #252526
Sidebar bg:          #1e1e1e
Main Content bg:     #1e1e1e
Panel border:        #333
Text primary:        #d4d4d4
Text secondary:      #888
Text muted:          #555
Accent blue:         #569cd6
Accent gold:         #e2b93d
Hover bg:            #2a2d2e
Selection bg:        #264f78
Active indicator:    #569cd6 (2px left border)
Error/deleted:       #f48771
Success/added:       #4ec9b0
```

### 9.2 Typography（微調 desktop 可讀性）

```
Sidebar labels:     13px, -apple-system
Code font:          14px, 'SF Mono', 'Fira Code', 'Cascadia Code', Menlo, monospace
Tour markdown:      15px body, 20px h2, 17px h3
Breadcrumb:         12px, #888, separator: /
```

### 9.3 Spacing

```
Activity bar icon:   48 x 48px (hitbox), icon 20px
Sidebar item:        height 28px, padding 0 12px
Sidebar section:     margin-top 16px, header 11px uppercase #888
Resize handle:       4px wide, visible on hover (border-right: 1px solid #569cd6)
Main content:        padding: 0 (code viewer 自管)
```

### 9.4 Animations

```
Sidebar collapse:    width 200ms ease-out
Panel resize:        none (instant, follow cursor)
Tab switch:          main content crossfade 150ms (keep existing)
Popover:             opacity + translateY 150ms ease-out
Hover:               background-color 100ms ease
```

Desktop view transition 詳細行為見 Section 6 的 view transition 對照表。

---

## 10. Side-by-Side Diff (Desktop Enhancement)

Desktop 有足夠寬度做雙欄 diff，取代 mobile 的 unified diff。

```
┌────────────────────────┬────────────────────────┐
│ OLD (commit parent)    │ NEW (commit)           │
│────────────────────────│────────────────────────│
│  10 │ import { A }     │  10 │ import { A }     │
│  11 │ import { B }     │  11 │ import { B }     │
│  12 │ import { C }     │  12 │ import { D }     │  ← red bg / green bg
│  13 │                  │  13 │ import { E }     │  ← added line
│  14 │ function foo() { │  14 │ function foo() { │
│  15 │   return bar     │  15 │   return baz     │  ← changed
│     │                  │  16 │   doExtra()      │  ← added
│  16 │ }                │  17 │ }                │
└────────────────────────┴────────────────────────┘
```

- 兩欄同步 scroll
- 左右分別標 old/new line numbers
- 刪除行：左側 red bg，右側空白
- 新增行：左側空白，右側 green bg
- 修改行：兩側都標色，可做 word-level diff highlight
- 可切換回 unified mode（toggle button）

### 10.1 Media Diff Preview

Diff detail 現已支援 media file preview（image/video）。Desktop diff viewer 需處理三種情況：

| 情境 | 顯示方式 |
|------|---------|
| Text file with hunks | Side-by-side text diff（上面的雙欄 layout） |
| Media file (image/video), working tree | Media preview（居中顯示，合理尺寸限制） |
| Added text file, no hunks | Synthetic "all lines added" text view |

Media diff 在 desktop 可以做更好的 before/after 比較：
- **Overlay slider**: 左右拉桿疊合新舊圖片
- **Side-by-side preview**: 新舊圖片並列（利用雙欄空間）
- **Toggle**: 點擊切換 old/new

目前 mobile 只顯示 current version preview，desktop 可以在有足夠空間時顯示 before/after 並列。

---

## 11. Implementation Phases

### Phase 1: Responsive Shell（核心骨架）
1. 新增 `useIsDesktop()` hook（`matchMedia(min-width: 768px)`）
2. 新增 `ActivityBar` component
3. 新增 `SidebarPanel` + `ResizeHandle` components
4. 修改 `TabLayout`：desktop 時 render `DesktopLayout`
5. Files tab: sidebar 顯示 file tree，main 顯示 code viewer
6. CSS: 隱藏 bottom tab bar on desktop

### Phase 2: 所有 Tab 適配
7. Git sidebar + main content split
8. Tours sidebar (with step list drill-down) + main content
9. Workspace popover
10. Empty states for each tab

### Phase 3: Desktop 增強
11. Side-by-side diff view
12. Hover states（所有互動元素）
13. Keyboard shortcuts
14. Code viewer breadcrumb navigation
15. Sidebar resize persistence

### Phase 4: 打磨
16. View transition 適配 desktop（tab switch crossfade 保留，page slide 改為 instant）
17. 移除桌面不需要的 mobile gestures
18. 測試：Chrome/Safari/Firefox desktop
19. 測試：iPad landscape（compact desktop mode）

---

## 12. Key Architectural Decisions

### Q1: Code structure — fork 還是 variant prop？

**決定**: Fork page-level code（做法 B）。同一個 app、同一個 port，`app.tsx` 是唯一分流點，`layouts/mobile/` 和 `layouts/desktop/` 各自獨立維護 pages。Services / hooks / pure UI components 共用。Desktop 穩定後再往共用層收斂。兩種做法的完整比較見 Section 8.2。

### Q2: Desktop routing 要改 route 結構嗎？

**決定**: 不改。URL 機制完全不動，desktop 只改 render 層。保持 `/files`, `/files/*`, `/git`, `/git/diff/*` 等現有 routes。所有 `navigate()` 都是 `push`，history stack 行為跟 mobile 一致。（詳見 Section 6）

### Q3: Sidebar 和 main content 之間怎麼通訊？

**決定**: 繼續用 React Router 的 URL 作為 single source of truth。
Sidebar 點擊 → `navigate('/files/path')`。
Main content 讀 route params render 內容。
不需要額外的 shared state。

### Q4: Desktop 怎麼利用新的 state services？

**建議**: 直接 import。Phase 2 state cleanup 已經把所有 per-workspace 狀態抽成獨立 module，desktop 不需要再重新實作。

- Session restore: `readLastLocation(workspace)` → 取得上次的 tab + route → `navigate()` 到正確位置
- File tree highlight: `readCurrentFile(workspace)` → 取得當前檔案 path
- Scroll restore: `readSavedFileScroll(workspace, path)` → code viewer 自動處理
- Workspace 切換: `selectWorkspace()` 後所有 state 自動切到新 workspace 的 scope

State services 都以 `workspaceKey` 為主鍵，並保留 `rootPath` / `extensionId` fallback migration。Desktop 不需要擔心 key collision。

### Q5: Deep link resolvers 在 desktop 怎麼顯示？

**建議**: `/open/*` routes 在 desktop main content 中渲染 workspace resolver UI，sidebar 保持當前 tab 的內容不動。Resolver 完成後 redirect 到目標 route（如 `/files/path`），此時 sidebar 自動切到對應 tab。

若 workspace 已連線且匹配，resolver 直接 redirect 不顯示 UI（零閃爍）。

---

## 13. Non-Goals (這次不做)

- **Editor tabs / multi-file open**: 暫時維持單檔案 focus，不做 tab strip
- **Split editor (左右兩個 code viewer)**: 複雜度高，先不做
- **Terminal panel**: 不是 code viewer 的範疇
- **Minimap**: 效能代價高，暫緩
- **Right panel (outline/references)**: 先保持在 popover/modal，之後再考慮固定 panel
