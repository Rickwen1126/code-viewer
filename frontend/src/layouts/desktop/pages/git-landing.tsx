/** Landing page for /git on desktop — placeholder until git sidebar is forked. */
export function GitLandingPage() {
  return (
    <div style={{ padding: 32, color: '#888', fontSize: 14 }}>
      Select a commit or changed file from the sidebar to view its diff.
    </div>
  )
}
