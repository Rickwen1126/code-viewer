/** Landing page for /tours on desktop — placeholder until tours sidebar is forked. */
export function ToursLandingPage() {
  return (
    <div style={{ padding: 32, color: '#888', fontSize: 14 }}>
      Select a tour from the sidebar to begin.
    </div>
  )
}
