// Home page — project selection (populated in Phase 5, T034)
export default function HomePage() {
  return (
    <div style={{ padding: "0 24px" }}>
      <h1
        style={{
          fontSize: 24,
          fontWeight: 600,
          color: "var(--text-primary)",
          marginTop: 24,
        }}
      >
        Open a Repo
      </h1>
      <p
        style={{
          color: "var(--text-tertiary)",
          fontSize: 13,
          marginTop: 8,
        }}
      >
        Project list will be available after Phase 5 implementation.
      </p>
    </div>
  );
}
